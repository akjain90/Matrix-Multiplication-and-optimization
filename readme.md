1) To run the code, please edit the file "likwidScript" to input the names of the matrices you would like to multiply.

2) Run the likwidScript using ./likwidScript

3) After running the code, you would be asked to enter a letter from a to e in order to select the implementation you want to run. (eg. 'a' to run the Naive code etc) 

